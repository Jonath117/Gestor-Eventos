# 1. Ejecutar lint-staged para el frontend (Biome)
pnpm lint-staged

# 2. Ejecutar dotnet format para el backend
# Apuntamos al archivo .sln que está en la carpeta backend/
dotnet format backend/backend.sln